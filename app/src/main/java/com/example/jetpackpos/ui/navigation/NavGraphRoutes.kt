package com.example.jetpackpos.ui.navigation

object NavGraphRoutes {
    const val ROOT_GRAPH = "root_graph" // Main graph if ever needed for further nesting
    const val POS_CHECKOUT_GRAPH = "pos_checkout_graph"
    // Add other graph routes here if the app grows
}
